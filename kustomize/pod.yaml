apiVersion: v1
kind: Pod
metadata:
  name: backdoor-demo
spec:
  containers:
    - image: xxradar/hackon
      name: certinject
      command: ["sleep"]
      args: ["240s"]
      resources: {}
      ports:
        - containerPort
