apiVersion: v1
kind: Pod
metadata:
  name: mitm-demo-pod-kustomize
spec:
  containers:
    - image: xxradar/hackon
      name: certinject
      command: ["sleep"]
      args: ["240s"]
      resources: {}
      ports:
        - containerPort: 80
  dnsPolicy: Default
